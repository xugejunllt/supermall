<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanf.pay.mapper.ReconciliationDiffMarkerMapper">

    <insert id="batchInsertIgnore">
        INSERT IGNORE INTO reconciliation_diff_marker (
        id,
        batch_id,
        business_order_no,
        diff_type,
        business_type,
        create_time,
        update_time
        ) VALUES
        <foreach collection="list" item="item" separator="," >
            (
            #{item.id},
            #{item.batchId},
            #{item.businessOrderNo},
            #{item.diffType},
            #{item.businessType},
            NOW(),
            NOW()
            )
        </foreach>
    </insert>

</mapper>
