<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanf.pay.mapper.ReconciliationDiffMapper">

    <insert id="batchInsertIgnore">
        INSERT IGNORE INTO reconciliation_diff (
        id,
        batch_id,
        business_order_no,
        pay_channel,
        expected_amount,
        actual_amount,
        expected_status,
        actual_status,
        diff_amount,
        diff_type,
        business_type,
        create_time,
        update_time
        ) VALUES
        <foreach collection="list" item="item" separator="," >
            (
            #{item.id},
            #{item.batchId},
            #{item.businessOrderNo},
            #{item.payChannel},
            #{item.expectedAmount},
            #{item.actualAmount},
            #{item.expectedStatus},
            #{item.actualStatus},
            #{item.diffAmount},
            #{item.diffType},
            #{item.businessType},
            NOW(),
            NOW()
            )
        </foreach>
    </insert>

    <delete id="deleteAll">
        DELETE FROM reconciliation_diff
    </delete>

</mapper>
