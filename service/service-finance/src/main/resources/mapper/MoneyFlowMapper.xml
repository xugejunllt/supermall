<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanf.finance.mapper.MoneyFlowMapper">


   <select id="sumIncomeMoney" resultType="double">

       SELECT
       IFNULL(SUM( income_money ) , 0.00)
       FROM
       money_flow
       WHERE
       shop_id = #{shopId}
       AND income =#{income}
       AND income_account=#{incomeAccount}
       <if test="startTime!=null">
           AND trade_finish_time >= #{startTime}

       </if>
       <if test="endTime!=null">
           AND trade_finish_time &lt;= #{endTime}
       </if>

   </select>

</mapper>
